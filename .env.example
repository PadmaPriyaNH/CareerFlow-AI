# Example environment variables for local docker-compose
SECRET_KEY=replace-this-with-a-secure-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Ollama (optional)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=mistral
AI_PROVIDER=ollama
AI_MODEL=mistral

# Local sqlite fallback
DB_NAME=db.sqlite3

# Example Docker Compose Postgres DB URL (used by docker-compose.yml)
# DATABASE_URL=postgres://careerflow:careerflow@db:5432/careerflow
